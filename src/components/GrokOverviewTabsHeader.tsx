import { PlatformOverviewTabsHeader } from './platform/PlatformOverviewTabsHeader';

export function GrokOverviewTabsHeader() {
  return <PlatformOverviewTabsHeader platform="grok" active="overview" tabs={['overview']} />;
}
