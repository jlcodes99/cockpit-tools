name: build-image

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: .
          target: novnc
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/cockpit-tools:nonvc-latest
            ghcr.io/${{ github.repository_owner }}/cockpit-tools:nonvc-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max